# Next Step

## Phase 4 当前目标
- 继续只处理主流程的必要反馈细节。
- 不扩展业务功能，不改统一重算路径，不做大规模视觉重做。

## 下一步任务顺序
1. 回归检查结果录入页的提示态、空态和可结束提示是否一致。
2. 回归检查本局确认页的禁用态、确认说明和无内容提示是否一致。
3. 回归检查 `hu-form` 的无候选提示、确认前说明与已有 toast 是否不冲突。
4. 若主流程反馈已稳定，再决定是否补极小的文本统一，不进入新一轮视觉扩张。

## 验收标准

### 主流程页面
- 结果录入页能清楚表达：
  - 当前是否已有结算
  - 当前是否可以进入确认页
  - 血流成河提示与一般提示不冲突
- 本局确认页能清楚表达：
  - 当前是否有可确认内容
  - 确认按钮何时可用
  - 返回修改与确认累计的优先级

### 表单反馈
- `hu-form` 中无可选胡牌人时有明确提示。
- 点炮模式下无可选点炮人时有明确提示。
- 确认前说明与现有 toast 校验逻辑一致。

### 工程约束
- 不修改核心业务逻辑。
- 不改统一重算路径。
- 不新增组件，除非现有结构无法承载。
- diff 保持小且可审查。
