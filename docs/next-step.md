# Next Step

## Phase 3 目标
- 收口剩余的 Phase 3 业务闭环。
- 重点补齐血流成河 V1 细节和撤销边界。
- 为 Phase 4 的视觉和反馈优化做准备。

## 任务拆分顺序
1. 仅进行必要的交互反馈补齐（非视觉精修）

## 验收标准

### 组件层
- 杠牌和查大叫录入入口可正常工作
- `hu-form` 支持血流成河下每位赢家独立番数/根数录入

### 页面层
- 结果录入页可完成一局主要结算流程
- 本局确认页确认后可进入明确的后续动作
- 血战到底和血流成河的关键差异在交互上可感知
- 血流成河下可理解“同一玩家可多次胡牌”

### 数据接线
- 杠牌、查大叫、胡牌都能写入 settlement
- 删除和撤销都能触发统一重算
- 确认后 `totalScores` 和 `currentRound` 状态正确更新
- 玩家名临时修改不污染持久化 `playerNames`
- 同一局多次胡牌（血流）不会污染之前 settlement
- 血流点炮多赢家不会共享同一组番数/根数

### 工程约束
- 页面内不直接实现核心计分逻辑
- 仍保持微信原生小程序结构
- 不引入新框架
- 代码可继续在微信开发者工具中演进

## 当前注意事项
- 继续复用现有 `navigation-bar` 组件
- 首页必须保留为独立 page
- `hu-form` 保持为 `round-entry` 页内组件，不改成独立 page
- 继续保持页面层不直接写计分逻辑

## Phase 4（预告）

将在 Phase 3 完全稳定后执行：

- Stitch 风格对齐（spacing / color / typography）
- 页面层级优化
- 状态反馈优化（空态 / loading / 操作提示）
