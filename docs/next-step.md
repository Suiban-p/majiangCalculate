# Next Step

## Phase 3 目标
- 完成录入页的业务闭环。
- 完成杠牌、查大叫、撤销的正式实现。
- 完成血战到底和血流成河 V1 的关键业务差异。
- 完成结果确认后的下一步流程。

## 任务拆分顺序
1. 完整处理血流成河一炮多响和多次胡牌
2. 完整处理血战到底自动结束后的交互提示
3. 补齐删除 settlement 后的统一重算边界
4. 收敛玩家名“临时修改”策略
5. 开始 Stitch 风格精修和状态反馈

## 验收标准

### 组件层
- 杠牌和查大叫录入入口可正常工作
- `hu-form` 支持血流成河下的一炮多响录入

### 页面层
- 结果录入页可完成一局主要结算流程
- 本局确认页确认后可进入明确的后续动作

### 数据接线
- 杠牌、查大叫、胡牌都能写入 settlement
- 删除和撤销都能触发统一重算
- 确认后 `totalScores` 和 `currentRound` 状态正确更新

### 工程约束
- 页面内不直接实现核心计分逻辑
- 仍保持微信原生小程序结构
- 不引入新框架
- 代码可继续在微信开发者工具中演进

## 当前注意事项
- 继续复用现有 `navigation-bar` 组件
- 首页必须保留为独立 page
- `hu-form` 保持为 `round-entry` 页内组件，不改成独立 page
- 继续保持页面层不直接写计分逻辑
