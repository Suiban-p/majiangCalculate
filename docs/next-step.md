# Next Step

## 当前目标
- 不再扩展功能或页面范围。
- 只进行主流程手工回归检查和极小的文本修正。

## 下一步任务顺序
1. 在微信开发者工具中回归主流程：
   - 规则配置页
   - 结果录入页
   - 胡牌登记表单
   - 本局确认页
   - 确认后的下一步动作
2. 检查以下文案是否实际显示正确：
   - 页面级空态
   - warning 提示
   - confirm 提示
   - disabled 反馈
   - 血战到底 / 血流成河模式说明
3. 如果发现显示或语气仍不一致，只做最小文本修正。

## 验收标准

### 主流程页面
- 结果录入页能清楚表达：
  - 当前还没有本局结算
  - 当前是否可以进入本局确认页
  - 血战到底和血流成河的规则差异
- 本局确认页能清楚表达：
  - 当前还没有可确认的结算
  - 确认按钮何时可用
  - 确认后会发生什么

### 表单反馈
- `hu-form` 中无可选胡牌人时有明确空态提示。
- 点炮模式下无可选点炮人时有明确空态提示。
- 表单内提示、toast 和页面提示的语气保持一致。

### 工程约束
- 不修改业务逻辑。
- 不改统一重算路径。
- 不新增组件。
- 若继续修改，只允许最小、可审查的文案级 diff。

## 2026-04-17 下一步：P0 回归复测
- 当前不扩展功能，不进入其它 UI 优化。
- 下一步只复测第二轮 P0 布局回归修复是否生效。

### 必测截图状态
1. `结果录入-血流成河空状态-2`
   - 验证累计记分板不再覆盖模式说明。
   - 验证页面滚动后正文内容完整可见。
2. `结果录入页-血战到底-2`
   - 验证累计记分板不再覆盖确认提示。
   - 验证本局实时分和本局记录区域仍可正常滚动查看。
3. `本局结果确认-1`
   - 验证底部 sticky 操作区不再压住本局记录。
   - 验证 `返回修改` / `确认并累计` 仍保持可达。
4. `本局结果确认-2`
   - 验证滚动到底部时最后一条 settlement 完整可见。
   - 验证底部操作区不被 home indicator 遮挡。
5. `首页-血战到底初始状态` 或 `首页-血流成河初始状态`
   - 快速确认取消记分板 sticky 后，首屏顶部仍不与状态栏 / 微信胶囊区重叠。

### 本轮通过标准
- 没有顶部胶囊遮挡。
- 没有累计记分板覆盖正文内容。
- `round-confirm` 最后一条结算记录可完整看到。
- `round-confirm` 底部确认按钮始终可达。
- 不因本轮修复引入新的明显遮挡。

## 2026-04-17 下一步：Final P0 回归复测
- 当前只复测 `round-confirm` 底部 sticky 操作区与结算记录列表的关系。
- 不扩展到表单密度、记分板高度、空态文案等非 P0 问题。

### 必测截图状态
1. `本局结果确认页1`
   - 验证 `本局记录` 卡片和最后一条 settlement 不再被底部 sticky 操作区压住。
   - 验证 `返回修改` / `确认并累计` 仍保持可达。
2. `本局结果确认页2`
   - 验证滚动到底部时最后一条 settlement 完整可见。
   - 验证底部操作区不被 home indicator 遮挡。

### Final P0 通过标准
- `round-confirm` 最后一条 settlement 可完整看到。
- sticky 底部按钮不覆盖结算记录内容。
- 底部按钮仍可达且不被 home indicator 遮挡。
- 不因本轮修复引入新的明显遮挡。
